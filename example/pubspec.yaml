name: super_auto_suggestion_box_example
description: Runnable gallery for the super_auto_suggestion_box Flutter package.

# version
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  # Shared GeniusLink foundation (transitive via the package; listed for resolution).
  super_core: ">=2.0.0 <3.0.0"
  super_auto_suggestion_box:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
